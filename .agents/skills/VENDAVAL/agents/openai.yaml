interface:
  display_name: "VENDAVAL"
  short_description: "Repo-specific patterns and workflows for VENDAVAL"
  default_prompt: "Use the VENDAVAL repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true